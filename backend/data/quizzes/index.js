// Central export for premade quizzes in the new consolidated location.
// Merge from legacy structure but replace question arrays with per-topic files
// when available, preserving the original ALL_QUIZZES shape.
module.exports = require('./index.dynamic.js');
